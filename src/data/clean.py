"""Cleaning & validation — implemented in Phase 2.

Parse timestamps, handle missing values, and enforce units (°C, mm, %, ...)
so the database holds a consistent, tidy record regardless of source.
"""
